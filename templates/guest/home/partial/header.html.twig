{# TOP BAR #}
<div class="bg-dark py-2 d-none d-md-block">
  <div class="container d-flex justify-content-between align-items-center text-white-50">
    <small><i class="fas fa-envelope mr-2"></i> usatum@gmail.com | <i class="fas fa-phone mx-2"></i> +229 XX XX XX XX</small>
    <div class="small">
      <a href="#" class="text-white-50 mx-2" onclick="alert('Contact: marnelginola@gmail.com')">Aide & Contact</a>
      <span class="mx-1">|</span>
      <a href="#" class="text-white-50 mx-2 text-decoration-none">Guide PDF</a>
    </div>
  </div>
</div>

{# MAIN NAVIGATION #}
<nav id="mainNav" class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
  <div class="container">
    <a class="navbar-brand font-weight-bold text-primary" href="#">
      <span class="text-dark">USAT</span><span class="text-success">UM</span>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto font-weight-bold text-uppercase small align-items-center">
        <li class="nav-item"><a class="nav-link px-3" href="#hero">Accueil</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="#ambition">Ambition</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="#procedures">Procédures</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="#recherche">Parcs & Agents</a></li>
        <li class="nav-item ml-lg-3">
          <button class="btn btn-primary btn-sm px-4 shadow-sm font-weight-bold" data-toggle="modal" data-target="#authModal">
            <i class="fas fa-user-circle mr-1"></i> CONNEXION
          </button>
        </li>
      </ul>
    </div>
  </div>
</nav>

{# HERO SECTION #}
{# HERO SECTION WITH IMAGE CAROUSEL (5 SECONDS) #}
<section id="heroSection" class="p-0 border-bottom">
  <div id="mainHeroCarousel" class="carousel slide carousel-fade" data-ride="carousel" data-interval="5000">
    {# Indicators #}
    <ol class="carousel-indicators">
      <li data-target="#mainHeroCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#mainHeroCarousel" data-slide-to="1"></li>
      <li data-target="#mainHeroCarousel" data-slide-to="2"></li>
    </ol>

    <div class="carousel-inner">
      {# Slide 1 #}
      <div class="carousel-item active">
        {# Conteneur principal avec hauteur définie #}
        <div class="d-flex align-items-center justify-content-center text-center text-white position-relative overflow-hidden" style="height: 75vh;">

          {# Gestion Intelligente de l'image (WebP avec Fallback JPG) #}
          <picture class="position-absolute w-100 h-100" style="top: 0; left: 0; z-index: -2;">

            <source srcset="{{ asset('build/images/home/car-fleet-1.webp') }}" type="image/webp">
            <img src="{{ asset('build/images/home/car-fleet-1.jpeg') }}" alt="USAT Benin Fleet" class="w-100 h-100" style="object-fit: cover;">
          </picture>

          {# Overlay sombre pour la lisibilité du texte (Remplace le linear-gradient) #}
          <div class="position-absolute w-100 h-100" style="top: 0; left: 0; background: rgba(0,0,0,0.5); z-index: -1;"></div>

          {# Contenu Texte #}
          <div class="container py-5" style="z-index: 1;">
            <h1 class="display-3 font-weight-bold mb-3">Sécurité & Transparence</h1>
            <p class="lead mb-5 px-lg-5">Optimisez l'importation de vos véhicules avec l'application stricte du code CIMA en République du Bénin.</p>
            <button class="btn btn-success btn-lg px-5 shadow-lg font-weight-bold border-0" data-toggle="modal" data-target="#authModal">S'ENREGISTRER MAINTENANT</button>
          </div>
        </div>
      </div>
      {# Slide 2 #}
      <div class="carousel-item">
        {# Conteneur principal avec hauteur définie #}
        <div class="d-flex align-items-center justify-content-center text-center text-white position-relative overflow-hidden" style="height: 75vh;">

          {# Gestion Intelligente de l'image (WebP avec Fallback JPG) #}
          <picture class="position-absolute w-100 h-100" style="top: 0; left: 0; z-index: -2;">
            <source srcset="{{ asset('build/images/home/car-fleet-2.webp') }}" type="image/webp">
            <img src="{{ asset('build/images/home/car-fleet-2.jpeg') }}" alt="USAT Benin Fleet" class="w-100 h-100" style="object-fit: cover;">
          </picture>

          {# Overlay sombre pour la lisibilité du texte (Remplace le linear-gradient) #}
          <div class="position-absolute w-100 h-100" style="top: 0; left: 0; background: rgba(0,0,0,0.5); z-index: -1;"></div>

          {# Contenu Texte #}
          <div class="container py-5" style="z-index: 1;">
            <h1 class="display-3 font-weight-bold mb-3">Dématérialisation Totale</h1>
            <p class="lead mb-5 px-lg-5">Gérez vos transferts du port vers les parcs de vente en quelques clics, sans tracasseries administratives.</p>
            <a href="#procedures" class="btn btn-primary btn-lg px-5 shadow-lg font-weight-bold border-0">DÉCOUVRIR LE SYSTÈME</a>
          </div>
        </div>
      </div>

      {# Slide 3 #}
      <div class="carousel-item">
        {# Conteneur principal avec hauteur définie #}
        <div class="d-flex align-items-center justify-content-center text-center text-white position-relative overflow-hidden" style="height: 75vh;">

          {# Gestion Intelligente de l'image (WebP avec Fallback JPG) #}
          <picture class="position-absolute w-100 h-100" style="top: 0; left: 0; z-index: -2;">
            <source srcset="{{ asset('build/images/home/car-fleet-3.webp') }}" type="image/webp">
            <img src="{{ asset('build/images/home/car-fleet-3.jpeg') }}" alt="USAT Benin Fleet" class="w-100 h-100" style="object-fit: cover;">
          </picture>

          {# Overlay sombre pour la lisibilité du texte (Remplace le linear-gradient) #}
          <div class="position-absolute w-100 h-100" style="top: 0; left: 0; background: rgba(0,0,0,0.5); z-index: -1;"></div>

          {# Contenu Texte #}
          <div class="container py-5" style="z-index: 1;">
            <h1 class="display-3 font-weight-bold mb-3">Zéro Faux Frais</h1>
            <p class="lead mb-5 px-lg-5">Une plateforme conçue pour protéger les acteurs contre les rançonnements et les surcoûts injustifiés.</p>
            <a href="#recherche" class="btn btn-warning btn-lg px-5 shadow-lg font-weight-bold border-0 text-white">RECHERCHER UN PARC</a>
          </div>
        </div>
      </div>
    </div>

    {# Controls #}
    <a class="carousel-control-prev" href="#mainHeroCarousel" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon p-3" aria-hidden="true"></span>
    </a>
    <a class="carousel-control-next" href="#mainHeroCarousel" role="button" data-slide="next">
      <span class="carousel-control-next-icon p-3" aria-hidden="true"></span>
    </a>
  </div>
</section>

{# URGENT ALERTS BAR #}
<div class="bg-warning py-3 shadow-sm border-bottom">
  <div class="container text-dark font-weight-bold">
    <marquee scrollamount="6"><i class="fas fa-exclamation-circle mr-2"></i> Urgent : En raison d’une défaillance mécanique détectée ce matin, tous les véhicules du parc doivent être immobilisés immédiatement jusqu’à nouvel avis pour inspection de sécurité obligatoire.</marquee>
  </div>
</div>