{{ form_start(form) }}
  {{ form_widget(form) }}
  <div class="row">
    <div class="col-md-12 text-right">
      <button type="submit" class="btn btn-success btn-inform-saver"><i
                class="fas fa-rss"></i> {{ button_label|default('Publier') }}</button>
    </div>
  </div>
{{ form_end(form) }}
