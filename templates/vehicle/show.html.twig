{% extends 'base-admin.html.twig' %}

{% block title %}Véhicule{% endblock %}

{% block page_title %}Véhicule{% endblock %}
{% block page_title2 %}Véhicule{% endblock %}
{% block page_subtitle %}{{ vehicle.brand.name }}{% endblock %}

{% block page_content %}
    <div class="row pb-3">
        <div class="col-12 col-sm-12 col-md-12">
            <a class="btn btn-default" href="{{ path('vehicle_index') }}"><i class="fas fa-reply"></i> Mes Véhicules</a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h3 class="card-title"><i class="fas fa-eye"></i> Véhicule</h3>
          {#<div class="card-tools">
              <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                  <i class="fas fa-minus"></i></button>
              <button type="button" class="btn btn-tool" data-card-widget="remove" data-toggle="tooltip" title="Remove">
                  <i class="fas fa-times"></i></button>
          </div>#}
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-12 col-sm-8 col-md-6 offset-md-3 offset-sm-2">
                    {% include 'vehicle/show_table.html.twig' %}
                </div>
                <div class="col-12 col-sm-8 col-md-6 offset-md-3 offset-sm-2">
                  {#{{ include('vehicle/_delete_form.html.twig', {label: 'Supprimer'}) }}#}
                    {#<a class="btn btn-primary float-right" href="{{ path('vehicle_edit', {'id': vehicle.id}) }}"><i class="fas fa-edit"></i> Editer</a>#}
                </div>
            </div>
        </div>
      {#<div class="card-footer">
          Footer
      </div>#}
    </div>
{% endblock %}
